<h1><%= @species.animal %></h1>

<table class="table">
  <tr style="text-align:left">
    <th>Date</th>
    <th>Latitude</th>
    <th>Longitude</th>
    <th></th>
  </tr>
  <% @sightings.each do |sighting| %>
  <tr>
    <td><%= sighting.date %></td>
    <td><%= sighting.latitude %></td>
    <td><%= sighting.longitude %></td>
    <td><a href="/sightings/<%= sighting.id %>/edit">Edit/Delete</a></td>

  </tr>
  <% end %>
</table>

</br>

<h3> Please fill out the form below to report a <%= @species.animal %> sighting:</h3>
<form role="form" class="form-inline" action="/sightings" method="post">
  <div class="form-group">  
    <input name="species_id" type="hidden" value="<%= @species.id %>">
  </div>
   <%= render('form') %>
   <input type="submit" value="Report Sighting">
</form>

<p><a href="/species">Return to home page</a></p>

<p><a href="/species/<%= @species.id %>"
  data-confirm="Are you sure you would like to delete this species?"
  data-method="delete"
  rel="nofollow">Delete <%= @species.animal %></a></p>
